@model IEnumerable<Loja.Models.Compra>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Data_compra)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fk_id_usuario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fk_id_item)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Data_compra)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fk_id_usuario)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fk_id_item)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
